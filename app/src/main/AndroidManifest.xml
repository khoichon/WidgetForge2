<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".WidgetForgeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WidgetForge"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WidgetForge.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget Configuration Activity -->
        <activity
            android:name=".ui.editor.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.WidgetForge">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- ===== AppWidget Providers ===== -->

        <!-- Text Widget Provider -->
        <receiver
            android:name=".widget.TextWidgetProvider"
            android:exported="true"
            android:label="@string/widget_text_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/text_widget_info" />
        </receiver>

        <!-- Image Widget Provider -->
        <receiver
            android:name=".widget.ImageWidgetProvider"
            android:exported="true"
            android:label="@string/widget_image_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/image_widget_info" />
        </receiver>

        <!-- GIF Widget Provider -->
        <receiver
            android:name=".widget.GifWidgetProvider"
            android:exported="true"
            android:label="@string/widget_gif_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gif_widget_info" />
        </receiver>

        <!-- Code Widget Provider -->
        <receiver
            android:name=".widget.CodeWidgetProvider"
            android:exported="true"
            android:label="@string/widget_code_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/code_widget_info" />
        </receiver>

        <!-- ===== Broadcast Receivers ===== -->

        <!-- Cross-Widget Communication Bus -->
        <receiver
            android:name=".receiver.WidgetCommunicationBus"
            android:exported="false">
            <intent-filter>
                <action android:name="com.widgetforge.WIDGET_EVENT" />
                <action android:name="com.widgetforge.WIDGET_CHANNEL_MESSAGE" />
            </intent-filter>
        </receiver>

        <!-- Screen State Receiver for Battery Optimization -->
        <receiver
            android:name=".receiver.ScreenStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ===== Services ===== -->

        <!-- Code Widget Render Service -->
        <service
            android:name=".service.CodeWidgetRenderService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- GIF Render Service -->
        <service
            android:name=".service.GifWidgetRenderService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
